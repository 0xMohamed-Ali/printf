#include "main.h"
/**
 * char_handle - Handling characters passed by the user
 * @chr: The character passed;
 * Return: Lenght;
 */
int char_handle(char chr)
{
	int length = 0;

	length = _putchar(chr);

	return (length);
}
